import type { GeneratedRecipe, IngredientInput } from '../types/recipe.types.js'

export async function generateRecipesWithGemini(
  _ingredients: IngredientInput[],
): Promise<GeneratedRecipe[]> {
  // TODO: Implement Gemini call and strict response parsing.
  return []
}
