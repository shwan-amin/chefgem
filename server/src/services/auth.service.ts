export async function registerUser(_email: string, _password: string): Promise<void> {
  // TODO: Hash password and create user.
}

export async function loginUser(_email: string, _password: string): Promise<string> {
  // TODO: Verify credentials and return JWT token.
  return ''
}
